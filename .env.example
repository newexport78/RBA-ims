# Copy to .env and set for production
DEBUG=False
SECRET_KEY=your-secret-key
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com
DATABASE_URL=postgres://user:pass@host:5432/dbname

# OTP email — pick one (production):
# 1) Amazon SES on ECS: USE_SES=true, verify DEFAULT_FROM_EMAIL in SES (same region as AWS_SES_REGION_NAME).
# USE_SES=true
# AWS_SES_REGION_NAME=ap-south-1
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
# 2) Or SendGrid: SENDGRID_API_KEY=...
# SENDGRID_API_KEY=
# Local: DEBUG=true → OTP prints to console.

EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Optional
OTP_EXPIRY_MINUTES=10
SESSION_COOKIE_AGE=3600
SESSION_SAVE_EVERY_REQUEST=True
